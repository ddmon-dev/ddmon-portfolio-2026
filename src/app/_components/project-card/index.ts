// 공개 API: 호출부는 이 폴더('./project-card')에서만 가져온다.
export { ProjectGallery } from './project-gallery';
export { DetailSection } from './detail-section';
export type { Project, ProjectImage } from './types';
